# Target Selection Handoff

Generic no-effect construction is complete. Formal Candidate creation is blocked until authorized target-binding facts are supplied.

Required facts only:

- provider/platform;
- environment class (`PRE_PRODUCTION` or `PRODUCTION_SHADOW_NO_EFFECT`);
- target identifier;
- host model (`SINGLE_HOST` or `MULTI_HOST_EXPECTED`);
- provider-native or equivalent ephemeral credential injection path for no-effect drills.

Do not supply any secret value in Chat or GitHub.
