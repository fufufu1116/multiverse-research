# Multiverse Hybrid v3.0 — Stage 7 Execution Conventions Freeze v1

Status: FROZEN BEFORE STAGE-7 SETTLEMENT EXECUTION
Track: All-Market Historical Economic Track
Date: 2026-08-19 JST
Parent audit snapshot: `a0360b1c5622b0664e8180186a40eca9827fc63e`

## Purpose

This document removes implementation ambiguity while preserving the already-audited Stage-3 through Stage-7 scientific rules. It introduces no additional profile, gate, template, stake policy, threshold, model, market, search dimension, or outcome-conditioned choice.

## 1. Segment-local bankroll paths

Each Stage-7 segment is evaluated as its own historical bankroll path using the Stage-6 frozen starting bankroll of `100000 JPY`:

- Segment A starts at 100000 JPY.
- Segment B starts at 100000 JPY.
- Segment C starts at 100000 JPY.

Bankroll is chronological within a segment and is not carried from A to B or B to C. This makes the A, B, and C metrics directly represent the configuration's performance on each preregistered segment under the same Stage-6 initial condition.

## 2. Immutable configuration identifier

The canonical configuration ID is:

`PROFILE:GATE:TEMPLATE:STAKE_POLICY`

The complete frozen configuration universe remains exactly 784 IDs generated by the Cartesian product already preregistered in Stages 3-6.

## 3. Affordability / unit-budget truncation ordering

When a frozen stake policy cannot fund every selected Stage-5 ticket because of available bankroll or whole-100-JPY unit limits, tickets are retained in the already-frozen Stage-4 global order:

1. higher consensus raw EV
2. higher consensus shape-edge ratio
3. higher consensus probability
4. lexicographically smaller market code
5. lexicographically smaller ticket key

No outcome information participates in truncation.

## 4. Exact fractional-Kelly operation order

For `FK10_R2` and `FK25_R3`, apply the Stage-6 wording literally and deterministically:

1. Compute each ticket's full Kelly fraction from conservative probability and the frozen price semantics.
2. Multiply by the frozen fractional-Kelly multiplier (0.10 or 0.25) to obtain the **uncapped raw fraction**.
3. Sum all positive uncapped raw fractions in the race.
4. If that sum exceeds the frozen race cap (0.02 or 0.03), scale **all positive uncapped raw fractions proportionally** so their sum equals the race cap.
5. Apply the frozen per-ticket cap (0.0025 or 0.005) to each scaled fraction.
6. Do not reallocate any budget made unused by the per-ticket cap.
7. Convert each ticket stake down to the nearest 100 JPY; omit zero-unit tickets.

This is one deterministic execution of the already-frozen caps and proportional-scaling rule, not a new policy candidate.

## 5. Segment C single-shot and technical restart semantics

`FINAL_DEV2000_CONFIGURATION_FREEZE_v1.json` must be atomically persisted before the Stage-7 evaluator opens the Segment-C settlement catalog.

After this freeze exists:

- A/B selection may never be recomputed to choose a different configuration.
- If a complete 500-race C ledger exists after an interruption, the evaluator must resume from that ledger and must not rescore C.
- If C execution was interrupted before an atomic complete ledger exists, a deterministic technical retry may execute only the identical already-frozen configuration. This is recorded as `technical_resume_same_frozen_trial=true`; it is not a new scientific trial and cannot change any rule.
- A completed C receipt or final receipt prohibits C rescoring.

The scientific Segment-C scoring count remains exactly 1 for the frozen lineage.

## 6. Bootstrap execution

Use the preregistered whole-race bootstrap:

- 10000 replicates
- seed `20260819`
- 500 Segment-C race rows sampled with replacement per replicate
- one process using a bounded multi-threaded worker pool
- deterministic resample-index generation occurs serially before worker dispatch
- batches are logged persistently during execution
- percentile bounds use NumPy `linear` percentile interpolation
- zero-total-stake replicates are omitted exactly as preregistered; fewer than 9500 valid replicates is FAIL-CLOSED

Thread completion order may vary, but bootstrap ROI results are reassembled by deterministic batch index before percentile calculation.

## 7. Outcome / HOLDOUT boundaries

The DEV2000 settlement bulk may physically recover A/B/C into separate settlement-only files under the independent Stage-7 authorization. The Stage-7 selection evaluator may read A, then B, but must not read Segment-C settlement content until the final configuration freeze is persisted.

`ECON_HOLDOUT1000` remains SEALED. No Candidate A/B1a refit, post-outcome Stage-3-6 rule modification, rescue tuning, live order generation, or real-money wagering is authorized.

END OF STAGE 7 EXECUTION CONVENTIONS FREEZE v1
