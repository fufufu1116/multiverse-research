name: Keirin Limited Synthetic Batch 1 v1 CI

on:
  pull_request:
    paths:
      - 'governance/KEIRIN_LIMITED_SYNTHETIC_BATCH1_RUN_MANIFEST_20260821_v1.json'
      - '.github/workflows/keirin-limited-synthetic-batch1-v1-ci.yml'

permissions:
  contents: read

jobs:
  synthetic-batch1:
    runs-on: ubuntu-latest
    env:
      AUTHORIZED_MAIN: '2496636a8782d195c7e7a7f8899dc9cfe2d7ed67'
      VNEXT_BLOB: 'afe746287c7e911d214202abdf2de076f63ab30c'
      KEIRIN_STATE_BLOB: '962d9c75b0611d52bd264a8b88bb1fe259cb8aeb'
      AUTH_RECEIPT_BLOB: '4c2e5364ad229a5dd413a19d0cf970c478692584'
      OWNER_GATE_BLOB: 'eba1c776da5efded3b99dfa2cb40271f79efa823'
      SYNTHETIC_SELFTEST_BLOB: '9fe59102bc799bb5620d94a580c6ac2568279eff'
      PYTHONDONTWRITEBYTECODE: '1'
    steps:
      - name: Checkout exact PR head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0

      - name: Fail closed unless canonical authorization is unchanged
        shell: bash
        run: |
          set -euo pipefail
          git fetch --no-tags origin main
          echo "PR_BASE_SHA=${{ github.event.pull_request.base.sha }}"
          echo "ORIGIN_MAIN=$(git rev-parse origin/main)"
          echo "PR_HEAD=$(git rev-parse HEAD)"
          test "${{ github.event.pull_request.base.sha }}" = "$AUTHORIZED_MAIN"
          test "$(git rev-parse origin/main)" = "$AUTHORIZED_MAIN"
          test "$(git rev-parse "$AUTHORIZED_MAIN:multiverse_vnext/VNEXT_CURRENT_STATE_v0.json")" = "$VNEXT_BLOB"
          test "$(git rev-parse "$AUTHORIZED_MAIN:v3/historical_all_market/governance/CURRENT_STATE_KEIRIN.json")" = "$KEIRIN_STATE_BLOB"
          test "$(git rev-parse "$AUTHORIZED_MAIN:governance/KEIRIN_LIMITED_SYNTHETIC_EXECUTION_AUTHORIZATION_RECEIPT_20260821_v1.json")" = "$AUTH_RECEIPT_BLOB"
          test "$(git rev-parse "$AUTHORIZED_MAIN:governance/KEIRIN_OWNER_GATE_LIMITED_SYNTHETIC_EXECUTION_20260821_v1.json")" = "$OWNER_GATE_BLOB"
          test "$(git rev-parse "$AUTHORIZED_MAIN:v3/historical_all_market/new_lineage/synthetic_selftest_v1.py")" = "$SYNTHETIC_SELFTEST_BLOB"

      - name: Verify Batch 1 changes no scientific code or data
        shell: bash
        run: |
          set -euo pipefail
          git diff --name-only "$AUTHORIZED_MAIN"...HEAD | sort > /tmp/actual.txt
          cat > /tmp/expected.txt <<'EOF'
          .github/workflows/keirin-limited-synthetic-batch1-v1-ci.yml
          governance/KEIRIN_LIMITED_SYNTHETIC_BATCH1_RUN_MANIFEST_20260821_v1.json
          EOF
          sort -o /tmp/expected.txt /tmp/expected.txt
          diff -u /tmp/expected.txt /tmp/actual.txt

      - name: Execute authorized source-independent synthetic Batch 1
        shell: bash
        working-directory: v3/historical_all_market/new_lineage
        run: |
          set -euo pipefail
          python synthetic_selftest_v1.py | tee /tmp/synthetic_batch1.log
          grep -Fx 'SYNTHETIC_SELFTEST_PASS' /tmp/synthetic_batch1.log

      - name: Protected-boundary terminal
        shell: bash
        run: |
          echo 'KEIRIN_LIMITED_SYNTHETIC_BATCH1_PASS'
          echo 'EVIDENCE_CLASS=SYNTHETIC_ENGINEERING_FALSIFICATION_ONLY'
          echo 'REAL_LIVE_INPUT_COLLECTION_PERFORMED=false'
          echo 'ECONOMICS_PERFORMED=false'
          echo 'REAL_WORLD_VALIDATION_PERFORMED=false'
          echo 'PR15_QUARANTINED_METRICS_INSPECTED=false'
          echo 'RESULT_PAYOUT_ACCESSED=false'
          echo 'HOLDOUT_ACCESSED=false'
          echo 'SCIENTIFIC_SEGMENT_C_SCORING_COUNT=0'
          echo 'MODEL_PROMOTION_PERFORMED=false'
          echo 'EXTERNAL_PROVIDER_CONTACT_PERFORMED=false'
          echo 'REAL_MONEY_WAGERING_PERFORMED=false'
          echo 'SYNTHETIC_IS_REAL_WORLD_EDGE_EVIDENCE=false'

      - name: Verify clean worktree
        run: test -z "$(git status --porcelain)"
