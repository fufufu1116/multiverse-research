name: Keirin Independent Official PRE Sensor — PAUSED

# OWNER-DIRECTED GLOBAL KEIRIN SAFE PAUSE — 2026-08-20 JST
# Prospective sensor collection/validation automation is disabled during foundation audit.
# Existing preregistration/captures remain preserved; no post-race backfill is authorized.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused-checkpoint:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm owner-directed global Keirin pause
        run: |
          echo "KEIRIN_RESEARCH_PAUSED_BY_OWNER_DIRECTIVE"
          echo "PR13_SENSOR_AUTOMATION_DISABLED"
          echo "PRE_PAUSE_HEAD=5b9bd82d99f2e2e322c93bb1b2c79c9243e84358"
          echo "NO_POST_RACE_BACKFILL_AS_PRE"
          echo "Do not restore sensor execution before the Multiverse foundation resume gate and Pause Guard conformance are satisfied."
