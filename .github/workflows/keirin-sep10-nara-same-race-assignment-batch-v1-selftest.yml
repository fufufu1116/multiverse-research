name: keirin-sep10-nara-same-race-assignment-batch-v1-selftest
on:
  push:
    paths:
      - '.github/workflows/keirin-sep10-nara-same-race-assignment-batch-v1-selftest.yml'
permissions:
  contents: write
jobs:
  selftest:
    if: github.ref == 'refs/heads/research/keirin-real-evidence-dual-lane-20260901-v1'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install --disable-pip-version-check requests beautifulsoup4
      - name: Run offline Sep10 Nara same-race assignment batch selftest
        shell: bash
        run: |
          set -o pipefail
          python tools/keirin_sep10_nara_same_race_assignment_batch_v1.py selftest \
            | tee v3/historical_all_market/research_candidates/KEIRIN_SEP10_NARA_SAME_RACE_ASSIGNMENT_BATCH_SELFTEST_20260904_v1.json
      - name: Persist research-only selftest evidence
        if: always()
        run: |
          f=v3/historical_all_market/research_candidates/KEIRIN_SEP10_NARA_SAME_RACE_ASSIGNMENT_BATCH_SELFTEST_20260904_v1.json
          test -f "$f" || exit 0
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add "$f"
          if git diff --cached --quiet; then exit 0; fi
          git commit -m 'research(keirin): persist Sep10 Nara assignment batch selftest v1'
          git push origin HEAD:${{ github.ref_name }}
