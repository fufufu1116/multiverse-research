name: Keirin Sep01-08 2026 Result Blind Universe Discovery v1
on:
  push:
    branches:
      - research/keirin-real-evidence-dual-lane-20260901-v1
    paths:
      - '.github/workflows/keirin-sep01-08-result-blind-universe-discovery-v1.yml'
permissions:
  contents: read
jobs:
  discover:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: python -m pip install --disable-pip-version-check beautifulsoup4
      - run: |
          mkdir -p out
          python tools/keirin_sep01_08_2026_result_blind_universe_discovery_v1.py \
            --out-csv out/KEIRIN_SEP01_08_2026_RESULT_BLIND_UNIVERSE_v1.csv \
            --receipt out/KEIRIN_SEP01_08_2026_RESULT_BLIND_UNIVERSE_RECEIPT_v1.json
      - uses: actions/upload-artifact@v4
        with:
          name: keirin-sep01-08-result-blind-universe-v1
          path: out/
          retention-days: 30
