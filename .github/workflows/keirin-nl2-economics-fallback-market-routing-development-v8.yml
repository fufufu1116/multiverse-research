name: Keirin NL2 Economics Fallback Market Routing Development v8 — DORMANT

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  authority-hold:
    runs-on: ubuntu-latest
    steps:
      - name: Fail closed — canonical Keirin authority is synthetic-only
        shell: bash
        run: |
          echo "DORMANT_DESIGN_ONLY: canonical main authority is synthetic-only."
          echo "No economics scoring, RESULT/PAYOUT access, fresh-target access, holdout opening, Runtime, or automatic betting is authorized."
          exit 1
