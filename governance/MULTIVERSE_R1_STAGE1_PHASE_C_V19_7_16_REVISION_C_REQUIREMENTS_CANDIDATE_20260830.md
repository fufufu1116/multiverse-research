# MULTIVERSE R1 STAGE 1 PHASE C v19.7.16 — REVISION C REQUIREMENTS CANDIDATE

Status: PROPOSED / NONCANONICAL / INDEPENDENT LAB READINESS REVIEW REQUIRED / NO EXECUTABLE AUTHORITY
Date: 2026-08-30 JST

## Basis
Revision B remains the governing accepted requirements until this candidate receives independent readiness PASS. Latest executable R3 Lab result is PR #74 comment 5466567413 = FIX_REQUIRED. The remaining material defect is requirements-level: Revision B simultaneously requires immutable historical runner bytes for the executable dependency and, in whole-loader tests, a synthetic runner substitute for launch/return/success behavior. The exact loader's runner Git-blob and SHA-256 trust gates make arbitrary synthetic runner substitution impossible without weakening/changing those gates.

## Revision C principle
Do not weaken the production/executable loader or historical-runner trust binding merely to make a test injectable. Separate **byte-identical complete-loader evidence** from **review-equivalent post-trust boundary evidence** and state exactly what each proves.

## Required evidence model
1. Classes 103..112 remain byte-identical complete exact-loader matrix cases. Each must begin at the exact frozen loader first byte, traverse every preceding real gate, and inject failure only through external observations while loader bytes remain unchanged.
2. Class 113 RUNNER_LAUNCH is primarily a static/mechanical exact-source proof over the byte-identical loader: exact immutable runner bytes must pass the loader's Bash `-n` gate in the success dependency model. A negative 113 execution case may use a separately frozen review-equivalent runner fixture only if explicitly labeled NON-BYTE-IDENTICAL and may prove only mapping/control-flow semantics, not production dependency behavior.
3. Class 114 RUNNER_RETURN and harmless success are tested at a formally defined **post-runner-trust handoff boundary**. The review-equivalent fixture must preserve the exact outer-loader statements from `PHASE_C_V19_7_15_RUNNER_START` through runner invocation, rc capture, RETURN fail mapping, and final exit; it may substitute only the child runner behavior after all immutable-runner identity/trust claims have already been proved separately against exact historical bytes.
4. The 114/success boundary fixture MUST be mechanically generated from exact frozen loader source by extracting the contiguous post-trust statement region using exact anchor tokens and must fail if anchors are missing, duplicated, reordered, or if generated bytes differ from the reviewed expected transformation. Hand-authored reimplementation is insufficient.
5. The immutable historical runner dependency remains exact recovery-head commit `19a14cfd019cceab199571b5d03d4dd0ba5bcd22`, Git blob `bc2b638b0db7fa8a0c23f0988cd9946f9e24b590`, SHA-256 `f4d91bb6fc73fbc236c49f0b364788ef8e7461850ff1bba1dd058d471e5468c2`. No executable candidate may replace/rebind it for live use.
6. 114 evidence must show RUNNER_START exactly once, synthetic child invocation exactly once, harmless child stdout/stderr preserved, nonzero child rc converted to fixed RETURN marker / outer114, no retry/fallthrough. Success evidence must show RUNNER_START once, child once, zero rc, no RETURN marker, outer0.
7. Unknown fallback115 may remain a separately frozen complete-source-equivalent dispatcher test, explicitly non-byte-identical, because no real exact-loader callsite intentionally supplies an unknown marker.
8. Every evidence artifact must carry an evidence-class field: `BYTE_IDENTICAL_COMPLETE_LOADER`, `GENERATED_POST_TRUST_BOUNDARY_EQUIVALENT`, or `SYNTHETIC_FALLBACK_EQUIVALENT`. No artifact may silently promote an equivalent fixture to byte-identical production evidence.
9. Existing Revision B requirements remain unchanged for exit map 103..115, collision proof, callsite binding, PASS-prefix binding, strict-prefix transport proof, main SHA/tree binding, device-code secrecy, no retry, full diagnostic chain, review sequencing and Runtime OFF, except where Revision C explicitly replaces Revision B items 8-9's requirement that 113/114/success all be injected through the byte-identical complete loader.
10. Revision C readiness PASS authorizes only a new executable remediation candidate. It does not retroactively PASS R3 and does not authorize Auditor, Codespace, OAuth/device flow, live execution or Runtime.

## Required Independent Lab readiness questions
- Is the Revision B 113/114/success contradiction correctly identified without weakening production trust?
- Is the generated post-trust boundary narrow enough to prove only outer handoff/return semantics while immutable-runner identity is independently proved?
- Are evidence classes explicit enough to prevent false equivalence claims?
- Does this close the material blocker in 5466567413 at requirements level?

## Non-authority
No executable acceptance, Auditor review, Codespace/live/OAuth/device flow, Step4, `--apply`, production/main/ruleset mutation, writer secret, merge, workflow dispatch, Runtime operation or activation. Runtime OFF.
