/usr/bin/env -i PATH='/usr/local/bin:/usr/bin:/bin:/usr/local/python/current/bin' HOME='/dev/shm/multiverse-r1-stage1-phase-c-home' TERM='xterm-256color' LANG='C' LC_ALL='C' CODESPACES="${CODESPACES:-}" CODESPACE_NAME="${CODESPACE_NAME:-}" GH_CONFIG_DIR='/dev/shm/multiverse-r1-stage1-phase-c-gh-auth' PYTHONNOUSERSITE='1' /bin/bash --noprofile --norc -c 'set -eu -o pipefail; umask 077; command printf "%s\n" "PHASE_C_RECOVERY_LOADER_START"; ORIGIN="https://github.com/fufufu1116/multiverse-research.git"; CANONICAL_MAIN="74ea95e59ac0654e1a0c1f811a178b3eef7b073c"; RECOVERY_HEAD="19a14cfd019cceab199571b5d03d4dd0ba5bcd22"; ROOT="/dev/shm/multiverse-r1-stage1-phase-c-recovery-control"; RHOME="/dev/shm/multiverse-r1-stage1-phase-c-recovery-control-home"; RTEMPLATE="/dev/shm/multiverse-r1-stage1-phase-c-recovery-control-template"; RUNNER="governance/MULTIVERSE_R1_STAGE1_PHASE_C_PRE_OAUTH_CONTINUITY_RECOVERY_RUNNER_20260827_v1.sh"; RUNNER_BLOB="bc2b638b0db7fa8a0c23f0988cd9946f9e24b590"; RUNNER_SHA256="f4d91bb6fc73fbc236c49f0b364788ef8e7461850ff1bba1dd058d471e5468c2"; command printf "%s\n" "PHASE_C_RECOVERY_LOADER_FRESH_PRECONDITIONS_START"; test "${CODESPACES:-}" = "true"; test -n "${CODESPACE_NAME:-}"; for p in "$ROOT" "$RHOME" "$RTEMPLATE" "$HOME" "$GH_CONFIG_DIR" "/dev/shm/multiverse-r1-stage1-phase-c-execution" "/dev/shm/multiverse-r1-stage1-phase-c-empty-git-template" "/dev/shm/multiverse-r1-stage1-phase-c-session-state" "/dev/shm/multiverse-r1-stage1-phase-c-recovery-step1.sh"; do test ! -e "$p"; test ! -L "$p"; done; command mkdir -m 700 "$ROOT" "$RHOME" "$RTEMPLATE"; for p in "$ROOT" "$RHOME" "$RTEMPLATE"; do test "$(command stat -c "%a" "$p")" = "700"; test "$(command stat -c "%u" "$p")" = "$(command id -u)"; fs="$(command stat -f -c "%T" "$p")"; test "$fs" = "tmpfs" || test "$fs" = "ramfs"; done; command printf "%s\n" "PHASE_C_RECOVERY_LOADER_FRESH_PRECONDITIONS_PASS"; git_clean() { command env -i PATH="/usr/local/bin:/usr/bin:/bin" HOME="$RHOME" LANG="C" LC_ALL="C" GIT_CONFIG_NOSYSTEM="1" GIT_CONFIG_SYSTEM="/dev/null" GIT_CONFIG_GLOBAL="/dev/null" GIT_ATTR_NOSYSTEM="1" GIT_NO_REPLACE_OBJECTS="1" GIT_TERMINAL_PROMPT="0" git "$@"; }; command printf "%s\n" "PHASE_C_RECOVERY_LOADER_CLONE_START"; git_clean clone --no-checkout --no-recurse-submodules --template="$RTEMPLATE" "$ORIGIN" "$ROOT"; test ! -L "$ROOT"; test "$(command stat -c "%a" "$ROOT")" = "700"; git_clean -C "$ROOT" config --local core.hooksPath /dev/null; command printf "%s\n" "PHASE_C_RECOVERY_LOADER_CLONE_PASS"; command printf "%s\n" "PHASE_C_RECOVERY_LOADER_REPO_BINDING_START"; test "$(git_clean -C "$ROOT" rev-parse --verify "refs/remotes/origin/main^{commit}")" = "$CANONICAL_MAIN"; git_clean -C "$ROOT" checkout --detach "$RECOVERY_HEAD"; test "$(git_clean -C "$ROOT" rev-parse --verify "HEAD^{commit}")" = "$RECOVERY_HEAD"; if git_clean -C "$ROOT" symbolic-ref -q HEAD >/dev/null 2>&1; then exit 88; else test "$?" -eq 1; fi; test "$(git_clean -C "$ROOT" remote)" = "origin"; test "$(git_clean -C "$ROOT" config --local --get remote.origin.url)" = "$ORIGIN"; test -z "$(git_clean -C "$ROOT" status --porcelain=v1 --untracked-files=all)"; command printf "%s\n" "PHASE_C_RECOVERY_LOADER_REPO_BINDING_PASS"; command printf "%s\n" "PHASE_C_RECOVERY_LOADER_RUNNER_BINDING_START"; entry="$(git_clean -C "$ROOT" ls-tree "$RECOVERY_HEAD" -- "$RUNNER")"; read -r mode type oid listed <<< "$entry"; test "$mode" = "100644"; test "$type" = "blob"; test "$oid" = "$RUNNER_BLOB"; test "$listed" = "$RUNNER"; test -f "$ROOT/$RUNNER"; test ! -L "$ROOT/$RUNNER"; test ! -x "$ROOT/$RUNNER"; test "$(command stat -c "%h" "$ROOT/$RUNNER")" = "1"; test "$(command stat -c "%u" "$ROOT/$RUNNER")" = "$(command id -u)"; perm="$(command stat -c "%a" "$ROOT/$RUNNER")"; (( (8#$perm & 022) == 0 )); test "$(git_clean -C "$ROOT" hash-object --no-filters -- "$RUNNER")" = "$RUNNER_BLOB"; set -- $(/usr/bin/sha256sum -- "$ROOT/$RUNNER"); test "$1" = "$RUNNER_SHA256"; export RECOVERY_ROOT="$ROOT"; command printf "%s\n" "PHASE_C_RECOVERY_LOADER_RUNNER_BINDING_PASS"; command printf "%s\n" "PHASE_C_RECOVERY_RUNNER_START"; set +e; /bin/bash --noprofile --norc "$ROOT/$RUNNER"; RUNNER_RC=$?; set -e; command printf "%s\n" "PHASE_C_RECOVERY_RUNNER_RETURNED_RC=$RUNNER_RC"; if [ "$RUNNER_RC" -ne 0 ]; then exit "$RUNNER_RC"; fi; command printf "%s\n" "PHASE_C_RECOVERY_RUNNER_RETURNED_ZERO"'