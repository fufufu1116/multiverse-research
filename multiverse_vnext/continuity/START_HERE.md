# MULTIVERSE Continuity — START HERE

This is the smallest entry point for a replacement AI/executor.

## 1. Read

1. `PROVIDER_INDEPENDENT_CONTINUITY_v1.md`
2. `CONTINUITY_MANIFEST_v1.json`
3. Run `python tools/build_continuity_packet.py` when execution is available.
4. Fresh Read canonical GitHub issue `#394` and implementation issue `#596`.
5. Fresh Read the default branch `main` before acting.

## 2. Treat as canonical

- GitHub `main` and the project control/review records are the durable source of current state.
- This START_HERE file, the manifest, and generated packets are navigation aids only.
- Chat transcripts are not canonical state.

## 3. Resume

Use the packet to identify the mission and first uncommitted step, then reconstruct volatile details from Fresh Read. Never replay an already receipted consequential effect.

## 4. If the previous AI is unavailable

Do not ask the Owner to summarize the lost chat if the canonical repository/control surfaces can answer the question. Rebuild the working context from the packet and Fresh Read.

## 5. If canonical state cannot be read

Remain fail-closed for consequential actions. A stale packet may describe the last known state, but it does not create authority or prove current state.

## 6. Provider neutrality

The replacement executor may be ChatGPT, Gemini, Claude, Codex, or another compatible system. The protocol is intentionally not coupled to a provider. Automatic switching is outside this candidate and requires separately authorized runtime/credential infrastructure.
